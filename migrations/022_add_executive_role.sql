-- =============================================================================
-- 役員ロールの追加
--
-- 取締役会資料・議事録、部署別人件費など機密性の高い情報を
-- 「管理者・役員」のみが閲覧できるようにするため、役員ロールを追加する。
--
-- 注: user_profiles.role は VARCHAR(20)（CHECK制約なし）のため、
--     列定義の変更は不要。user_roles マスタへの追加のみで足りる。
-- =============================================================================

INSERT INTO user_roles (code, name, description, display_order) VALUES
    ('executive', '役員', '経営情報（取締役会資料・人件費など）の閲覧が可能', 3)
ON CONFLICT (code) DO NOTHING;
